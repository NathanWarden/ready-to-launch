[plugin]

name="PostBuild"
description="Runs processes on builds after they are done such as increasing the build number or unzipping the Mac build."
author="Nathan Warden"
version="1.0"
script="PostBuildSetup.cs"
